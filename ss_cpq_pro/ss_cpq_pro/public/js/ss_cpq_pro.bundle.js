// ss_cpq_pro
// This is the app-level JS bundle entry point. Frappe's esbuild asset
// builder expects at least one <app_name>.bundle.js file to exist under
// public/js, even for apps (like this one) that don't ship global JS —
// without it, `bench build` fails with a "paths[0] undefined" error.
